//api.js
var http = require("http");
var url = require('url');
var express = require('express');

exports.loggedIn = function(req, res, param){
  var parsedUrl = url.parse(req.url, true);
  res.sendfile(pubHtmlFolder + '/feed.html');
};
